# Benchmark system

List all benchmarks
```sh
cargo run --release
```

Run all benchmarks
```sh
cargo run --release -- bench
```

Run a specific benchmark
```sh
cargo run --release -- bench preview_code
```
